{
  "name": "sudo requires password",
  "description": "Repo for the 'sudo requires password' module",
  "type": "module-repo",
  "provides": {
    "sudo-requires-password": {
      "description": "Ensure that sudo requires password",
      "tags": ["security"],
      "dependencies": ["autorun"],
      "steps": [
        "copy ./sudo-requires-password.cf services/autorun/sudo-requires-password.cf"
      ]
    }
  }
}
